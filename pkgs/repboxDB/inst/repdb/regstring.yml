name: regstring

descr: |
  Additional regression information stored as strings like those provided by broom::glance.
  Will not contain contain numeric and boolean values, which are stored in reg_stats instead.

unique_index:
  main_index: [runid, variant, string_name]

fields:
  runid:
    type: int

  variant:

  string_name:

  string_val:
