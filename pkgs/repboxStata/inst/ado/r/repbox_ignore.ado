program define repbox_ignore, properties(prefix)
  // Intentionally do nothing.
  // The properties(prefix) allows this to be called via "repbox_ignore: command"
  di "command is skipped by repbox to save time"
end
