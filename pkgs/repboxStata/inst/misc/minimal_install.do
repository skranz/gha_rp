* Install those Stata packages that are minimally needed

* Set installation directory
cd C:/libraries/repbox/ado/plus

ssc install rcall, replace
ssc install parmest, replace
ssc install esttab, replace
ssc install ivreg2, replace
